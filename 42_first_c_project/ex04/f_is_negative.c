//This code checks if the given integer bigger than 0 or not.
//If the number is positive, output will be "P".

#include <unistd.h>

void	ft_is_negative(int n)
{
	if (n < 0)
	{
		write(1, "N", 1);
	}
	else
	{
		write(1, "P", 1);
	}
}
